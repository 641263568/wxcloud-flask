version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.development
    volumes:
      - .:/app
    ports:
      - '62830:80'
    container_name: wxcloud_wxcloudrun-flask
    labels:
      - wxPort=27082
      - hostPort=62830
      - wxcloud=wxcloudrun-flask
      - role=container
    environment:
      - MYSQL_USERNAME=
      - MYSQL_PASSWORD=
      - MYSQL_ADDRESS=
networks:
  default:
    external:
      name: wxcb0
